// localpulse/app/src/constants/theme.js
// Shared sizing constants, matching the Recover / copd_doctor pattern so the
// PIN screens and other shared components can import FontSize / Spacing.

export const FontSize = {
  xs: 11,
  sm: 13,
  md: 15,
  lg: 18,
  xl: 24,
  xxl: 32,
};

export const Spacing = {
  xs: 4,
  sm: 8,
  md: 12,
  lg: 16,
  xl: 24,
  xxl: 32,
};
